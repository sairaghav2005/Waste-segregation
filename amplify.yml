version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies..."
    build:
      commands:
        - echo "Build complete - static files ready"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
